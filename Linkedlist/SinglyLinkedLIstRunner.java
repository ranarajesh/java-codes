package Linkedlist;

public class SinglyLinkedLIstRunner {
    public static void main(String[] args) {
        LinkedList<Integer> llist = new LinkedList<>();
        llist.insertNodeAtHead(10);
        llist.insertNodeAtHead(30);
        llist.insertNodeAtHead(40);
        llist.printList();
    }
}
